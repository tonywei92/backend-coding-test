# Quickstart

Ensure `node (>8.6 and <= 10)` and `npm` are installed, node `v10.22.0 lts` is highly recommended.

## installation

Run installation

```sh
$ npm install
```

Run server

```sh
$ npm start
```

## Testing

```sh
$ npm test
```

## Load testing

```sh
$ npm run test:load
```
